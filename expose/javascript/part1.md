1. It would print "values added: 20"
2. It would print "final result: 20"
3. You should not use var because in a sense it is not one time use. Whatever changes you make to the var in a the last function will be what the var starts as in the next function call.
4. It would print "values added: 20"
5. You would get an error since result in not defined in the same scope that line 13 occurs.
6. I think you would get an error since you are trying to reassign result after you assign it. However, since you declared result as const you can't do this so you would get an error. That means when you try to print result in line 9 it would give an error before it even reached that line.
7. Same explanation as answer 5.